# Store assets

Screenshots captured from a real, unpacked-extension Chromium session
(logged out) as a functional verification pass — see the repo README's
"Publishing to the Chrome Web Store" section for context.

- `popup.png` — the extension popup, all toggles on. Good as-is for the
  listing.
- `youtube-search.png` — search results for "news" with 0 visible
  `/shorts/` links. Good as-is for the listing.
- `youtube-watch.png` — a watch page with 0 visible `/shorts/` links in
  up-next and the Autoplay toggle fully hidden. Good as-is for the
  listing.

Still missing: a home-feed screenshot showing Shorts-shelf/Mixes
blocking. Needs a signed-in session (logged-out Chrome only shows a
"Try searching to get started" placeholder, nothing to demonstrate) —
see `STORE_LISTING.md`'s screenshot list.
